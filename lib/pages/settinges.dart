import 'package:flutter/material.dart';

class Settinges extends StatelessWidget {
  const Settinges({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text('settings'));
  }
}